{"id":"moving-tag-drift","component":"chain","signal":"EL newPayload INVALID / 'failed to decode BAL: EOF'; CL head frozen, low peers; bumping EL image alone did not help","class":"image-tag-drift","root_cause":"ethpandaops moving :<devnet> tags were re-pushed; the CL-side image was stale and mis-relayed the payload to the engine API","remediation":"compare pod image digest to the current moving tag FIRST; bump the stale client (here the CL) to the freshly re-pushed digest","expected_time_to_green":"minutes after rollout","trap":"debugging it as our EL decoder bug and chasing the EL when the fix was on the CL"}
{"id":"cl-zero-peers","component":"chain","signal":"CL head stuck, 1 peer (target ~70), '0 peers found to publish attestation'; pods all Running 1/1","class":"infra-retryable","root_cause":"CL lost peers; EL had nothing to drive so head froze — pod status was a false green","remediation":"kubectl rollout restart the CL; it re-discovers peers and drives the head","expected_time_to_green":"minutes","trap":"trusting 'Running 1/1' instead of looking at chain data (peer count, finalized epoch)"}
{"id":"wrong-cli-branch-sr-struct","component":"csm","signal":"module activation omnibus reverts bare on StakingRouter.addStakingModule; addresses have code (not a mismatch)","class":"wrong-branch","root_cause":"in-pod CLI was on an old branch calling the legacy 8-arg addStakingModule selector; deployed SR uses the struct-based signature (different selector)","remediation":"re-checkout the CLI to the branch with the struct-based signature + reinstall; omnibus then encodes the struct","expected_time_to_green":"minutes","trap":"patching the omnibus ABI by hand (fragile) instead of fixing the branch"}
{"id":"oracle-flat-bid-type","component":"oracles","signal":"reporter daemons CrashLoopBackOff: TypeError SignedExecutionPayloadBid.__init__() missing required positional arguments, in consensus types parsing","class":"upstream-code-bug","root_cause":"oracle branch modeled the signed bid flat; the CL emits it as {message, signature} per the canonical signed form — a code bug in the branch, not our node","remediation":"human — flag the owning oracle team; re-pull the branch once fixed and rebuild","expected_time_to_green":"depends on owning team","trap":"swapping node pairs — an infra remedy for a code fault wastes 50-min redeploys"}
{"id":"cmv2-oracle-not-member","component":"oracles","signal":"CMv2 reporter daemons crash with IsNotMember; consensus contract has 0 members","class":"governance-gate","root_cause":"the activation omnibus does not register consensus members or grant submit role; those live on the DAO Agent, the deployer cannot add them directly","remediation":"human — recommend an omnibus that addMember + grants SUBMIT_DATA_ROLE, then vote","expected_time_to_green":"vote + convergence cycle","trap":"restarting the daemons; the gap is on-chain, not in the pod"}
{"id":"oracle-initialepoch-too-low","component":"oracles","signal":"perf collector crash-loops requesting pre-checkpoint beacon states; demand floor points below the checkpoint","class":"config-front-loadable","root_cause":"HashConsensus initialEpoch was set below the checkpoint epoch; floor = initialEpoch - frame lands in history no node in the network serves","remediation":"front-load: set initialEpoch >= checkpoint at setup. If already past: retrofit = deploy a fresh HashConsensus + setConsensusContract + vote (updateInitialEpoch locks once the epoch arrives)","expected_time_to_green":"free if front-loaded; retrofit is a convergence cycle","trap":"expecting updateInitialEpoch to fix a live consensus — it is locked after the epoch arrives"}
{"id":"floor-not-moving-after-swap","component":"oracles","signal":"right after a consensus re-init/swap the demand floor still shows the old anchor; wiping the daemon DB does not move it","class":"converging-wait","root_cause":"the daemon and perf-collector cache the consensus address; they re-read the new one only after a convergence cycle","remediation":"wait (restart + let it converge); it can settle overnight","expected_time_to_green":"a convergence cycle, up to overnight","trap":"concluding the swap 'did not take' and redeploying — it converged on its own by morning"}
{"id":"sanity-balance-increase-revert","component":"oracles","signal":"submitReportData reverts with IncorrectTotalCLBalanceIncrease(maxAllowed, actual)","class":"governance-gate","root_cause":"the accounting oracle accrued many frames; the packed CL balance increase exceeds annualBalanceIncreaseBPLimit; it will not self-resolve (actual > cap, gap grows each frame)","remediation":"human — recommend a bump-sanity-limits omnibus (raise the limit), or do not let the oracle accrue frames","expected_time_to_green":"vote + next frame","trap":"waiting for it to clear on its own; the gap only widens"}
{"id":"besu-archive-backward-sync","component":"oracles","signal":"newly added archive EL pair not yet serving historical getBalance at the depth the heavy oracle report needs","class":"converging-wait","root_cause":"besu FOREST archive backward-syncs from the CL checkpoint to genesis; the deep state window fills gradually","root_cause_note":"expected, not a fault","remediation":"wait for backward-sync to reach the needed depth","expected_time_to_green":"~1 h","trap":"swapping the node pair mid backward-sync"}
